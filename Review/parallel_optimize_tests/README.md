# Parallel Optimize Review Tests

This directory is for review work based on `origin/refactor/parallel-optimize`.

- `tests/`: test code and helper scripts for validating the branch changes.
- `reports/`: test results, review notes, and benchmark records.

Keep this directory limited to review/test artifacts so the collaborator's
optimization changes remain easy to separate from validation work.
